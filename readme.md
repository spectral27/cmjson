### Build:
```shell
mvn package
```

### Usage:

Select all:
```shell
./cmjson -s
```

Select by id:
```shell
./cmjson -s <id>
```

Insert:
```shell
./cmjson -i <jsonfile>
```

Update:
```shell
./cmjson -u <jsonfile>
```

Delete:
```shell
./cmjson -d <id>
```
